export enum SortDirection {
  ASC = 'asc',
  DESC = 'desc'
}

export enum AvailableProtocols {
  Tcp = 'tcp',
  Http = 'http1',
  Http2 = 'http2',
  AllHttp = 'http.*'
}

export enum TcpStatus {
  Active = 'active',
  Terminated = 'terminated'
}
