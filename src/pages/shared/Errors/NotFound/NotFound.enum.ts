export enum NotFoundRoutesPaths {
  NotFound = '*' //The global route '*' is used to handle cases where the requested page is not found
}

export enum NotFoundLabels {
  ErrorTitle = 'Route not found'
}
