export enum ServicesRoutesPaths {
    Services = '/services',
    Overview = '/services/overview',
}

// SERVICES VIEW
export enum ServicesOverviewColumns {
    Name = 'Name',
    Protocol = 'Protocol',
}
